"""Camada `controladores` — routers FastAPI (camada de apresentação).

Cada módulo expõe um `router` com os endpoints de um domínio. As rotas seguem o
contrato documentado em `frontend_dashboard/js/api.js`.
"""
